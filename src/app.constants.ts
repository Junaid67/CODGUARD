/**
 * Application-wide constants.
 */
export const CONSTANTS = {
  API_VERSION: 'api/v1',
  APP_NAME: 'COD Risk Scorer',
  SWAGGER_PATH: 'docs',
};

/**
 * Metadata keys used by decorators + guards.
 */
export const METADATA_KEYS = {
  IS_PUBLIC: 'isPublic',
  AUDIT_ACTION: 'auditAction',
};
